server:
   port: 8080
   tomcat:
      uri-encoding: utf-8
spring:
   jackson:
      time-zone: GMT+8
   datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: 'jdbc:mysql://cdb-mzvws756.cd.tencentcdb.com:10143/nlp?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf8'
      username: nlp
      password: 'iop890*()'
      max-idle: 10
      max-wait: 10000
      min-idle: 5
      initial-size: 5
thymeleaf:
   prefix: classpath:templates/
   suffix: .html
   cache: false #关闭缓存
mybatis:
   mapper-locations: classpath:mapper/*.xml